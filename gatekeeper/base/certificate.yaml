apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gatekeeper
spec:
  secretName: gatekeeper-tls
  dnsNames:
    - "login.outi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer

