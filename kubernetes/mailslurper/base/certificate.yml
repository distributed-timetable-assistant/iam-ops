apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mailslurper
spec:
  secretName: mailslurper-tls
  dnsNames:
    - "mail.st.dita.hasankarimi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer

