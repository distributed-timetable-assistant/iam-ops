apiVersion: v1
kind: Service
metadata:
  name: kratos-auth
  labels:
    app: kratos-auth
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
  selector:
    app: kratos-auth

