apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kratos
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - kratos.st.dita.hasankarimi.ir
      secretName: kratos-tls
  rules:
    - host: kratos.st.dita.hasankarimi.ir
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kratos-public
                port:
                  name: http

