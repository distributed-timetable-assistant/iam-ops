apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kratos
spec:
  secretName: kratos-tls
  dnsNames:
    - "kratos.st.dita.hasankarimi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer

