apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ory-selfservice-rust
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - identity.outi.ir
      secretName: ory-selfservice-rust-tls
  rules:
    - host: identity.outi.ir
      http:
        paths:
          - path: /self-service
            pathType: Prefix
            backend:
              service:
                name: kratos
                port:
                  name: public
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ory-selfservice-rust
                port:
                  name: http
