apiVersion: apps/v1
kind: Deployment
metadata:
  name: ory-selfservice-rust
spec:
  revisionHistoryLimit: 0
  replicas: 1
  selector:
    matchLabels:
      app: ory-selfservice-rust
  template:
    metadata:
      labels:
        app: ory-selfservice-rust
    spec:
      containers:
        - name: ory-selfservice-rust
          image: backendd/dita.ory-selfservice-rust:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: ORY_UI__LISTEN_ADDR
              value: "0.0.0.0:8080"
            - name: ORY_UI__PUBLIC_BASE_URL
              value: "https://identity.outi.ir"
            - name: ORY_UI__KRATOS_PUBLIC_URL
              value: "http://kratos:4433"
            - name: ORY_UI__HYDRA_ADMIN_URL
              value: "http://hydra:4445"
            - name: RUST_LOG
              value: debug
          ports:
            - name: http
              containerPort: 8080
          resources:
            limits:
              cpu: 100m
              memory: 128Mi
            requests:
              cpu: 0m
              memory: 0Mi