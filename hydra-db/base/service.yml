apiVersion: v1
kind: Service
metadata:
  name: hydra-db
  labels:
    app: hydra-db
spec:
  type: ClusterIP
  ports:
    - name: tcp
      protocol: TCP
      port: 5432
      targetPort: tcp
  selector:
    app: hydra-db

