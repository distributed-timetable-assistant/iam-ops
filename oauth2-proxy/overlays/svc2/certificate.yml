apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oauth2-proxy-svc2
spec:
  dnsNames:
    - svc2.login.outi.ir
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: svc2-tls
