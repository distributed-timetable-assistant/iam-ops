apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: oauth2-proxy
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - grafana.login.outi.ir
      secretName: grafana-tls
  rules:
    - host: grafana.login.outi.ir
      http:
        paths:
          - path: /oauth2/grafana
            pathType: Prefix
            backend:
              service:
                name: oauth2-proxy
                port:
                  name: http
