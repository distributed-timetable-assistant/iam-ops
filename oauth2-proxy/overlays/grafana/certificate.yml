apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oauth2-proxy-grafana
spec:
  dnsNames:
    - grafana.login.outi.ir
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: grafana-tls
