apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-proxy
spec:
  selector:
    matchLabels:
      app: oauth2-proxy-monitor
  template:
    metadata:
      labels:
        app: oauth2-proxy-monitor
    spec:
      containers:
        - name: oauth2-proxy
          envFrom:
            - secretRef:
                name: oauth2-proxy-monitor
          args:
            - --provider=oidc
            - --oidc-issuer-url=https://auth.outi.ir
            - --redirect-url=https://monitor.login.outi.ir/oauth2/monitor/callback
            - --proxy-prefix=/oauth2/monitor
            - --email-domain=*
            - --http-address=0.0.0.0:4180
            - --upstream=static://200
            - --cookie-secure=true
            - --cookie-samesite=lax
            - --cookie-name=_oauth2_proxy_monitor
            - --set-xauthrequest=true
            - --pass-authorization-header=true
            - --pass-access-token=true
            - --skip-provider-button=true
