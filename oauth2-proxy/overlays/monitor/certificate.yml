apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oauth2-proxy
spec:
  dnsNames:
    - monitor.login.outi.ir
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: monitor-tls
