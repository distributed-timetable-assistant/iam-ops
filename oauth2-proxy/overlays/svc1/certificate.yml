apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oauth2-proxy-svc1
spec:
  dnsNames:
    - svc1.login.outi.ir
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: svc1-tls
