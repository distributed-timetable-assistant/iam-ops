apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ory-selfservice
spec:
  secretName: ory-selfservice-tls
  dnsNames:
    - "web.login.outi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
