apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hydra
spec:
  secretName: hydra-tls
  dnsNames:
    - "hydra.st.dita.hasankarimi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer

